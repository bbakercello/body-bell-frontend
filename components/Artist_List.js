import React from 'react'

const Artist_List = (props) => {
    // console.log(props.HEROKU)
  return (
    <div>
        
        <a href={props.HEROKU}>Heroku</a>
    </div>
  )
}

export default Artist_List